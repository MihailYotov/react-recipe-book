import React from 'react';

import Navigation from '../../components/Navigation/Navigation';

const header = () => {
    return (
        <div>
            <Navigation/>
        </div>
    );
};

export default header;